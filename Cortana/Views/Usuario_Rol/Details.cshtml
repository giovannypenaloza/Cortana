@model Cortana.Models.usuariorol

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<<div>
    <h4>usuariorol</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.roles.descripcion)
        </dt>

        <dd>
            @Cortana.Controllers.UsuarioRolController.Descripcion_Rol(Model.idRol)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.usuario.nombre)
        </dt>

        <dd>
            @Cortana.Controllers.UsuarioRolController.Nombre_Usuario(Model.idUsuario)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.id }) |
    @Html.ActionLink("Back to List", "Index")
</p>